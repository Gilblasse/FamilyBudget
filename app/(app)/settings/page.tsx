import { SettingsView } from '@/components/settings/settings-view';

export const metadata = {
  title: 'Settings · Family Budget',
};

export default function SettingsPage() {
  return <SettingsView />;
}
